enum March { LEFT, RIGHT }
public class Defense {
	enum March { LEFT, RIGHT }
	class Secret {
		// enum March { LEFT, RIGHT }
	}
	static class Open {
		enum March { LEFT, RIGHT }
	}
	public static void declareWar() {
		//enum March { LEFT, RIGHT }
		class Defense1 {
		}
	}
}